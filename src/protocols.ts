export type WidthType = {
  width: number | undefined;
};

export type EmployeeDataType = {
  id: number;
  name: string;
  job: string;
  admission_date: string;
  phone: string;
  image: string;
};
